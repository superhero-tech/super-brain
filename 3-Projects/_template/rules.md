# [Project name] - Rules

> Constraints that apply to this project only. They override the agent's defaults in `8-System/brain.md`.
> The human owns this file. The AI may suggest additions but never edits it unasked.

---

## Hard rules

<!-- Things the AI must never do here. Example:
- Never write to the production database, read replicas only
- Never put a client name in a file that lands outside this folder
-->

None yet.

## Working agreements

<!-- How you want this project run. Example:
- Every recommendation comes with a kill criterion
- Decisions go in decisions/ as a one-pager before anything gets built
-->

None yet.

## Vocabulary

<!-- Project-specific terms, so the AI uses your words. Example:
- "seat" = a named user, not a login
-->

None yet.
